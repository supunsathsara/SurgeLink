# Use the official Supabase image as the base image
FROM supabase/postgres:latest

# Set environment variables
ENV POSTGRES_DB=supabase
ENV POSTGRES_USER=supabase
ENV POSTGRES_PASSWORD=supabase

# Expose the port the database runs on
EXPOSE 5432